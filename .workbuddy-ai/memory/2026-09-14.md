# 2026-09-14 工作日志（GLM / LIMS 项目收尾）

## 结论：项目功能完工（进度 96% → 99%）

用户指令：读全部项目文件，找出「还需要做什么 / 什么是没用的了 / 已完成部分是否漏掉或空壳 /
功能交互是否彻底闭环」，彻底解决并提交 git 完工。

## 盘点结论（先复核「已做完的」，避免返工）

- **说明书 13 项功能全部落地**：RBAC / 方法资质 / 项目标准库 / 监抽任务 / 采样单导入 /
  项目分解 / 任务安排 / 检验员任务查询+导出 / 结果录入+自动判定 / 审核签发 /
  CMA·CMA-CATL 报告生成 / 在检·历史·项目库查询 / 省平台上报导出。
- **前端 17 页 100% 迁移公共组件**——交接单说「还剩 6 页」，实测是**过期状态**（9-13 23:xx 已迁完）。
- **P1~P5 巡检问题全修**（含 P3 列宽，交接单说未修，实测已修）。
- **权限标识代码 ↔ seed 逐个比对无缺口**。

## 本轮实际修复

1. **T-918 操作日志（消除空壳）**：`sys_operation_log`（db/init/09 + V7）+ `OperationLogInterceptor`
   + `GET /api/sys/log/page` + 契约第 15 章 + 前端真实表格 + 6 项单测。
2. **顶部铃铛去假数据**：4 条写死假通知 → 6 个业务域真实待办汇总（零值不展示、点击直达）。
3. **死代码**：`AppSkeleton` 接入工作台 KPI 加载态；`ProgressBar` 零引用 → 删除。
4. **Git 对象库修复**（见下）。

## 关键判断

- **操作日志不做 AOP 而做 HandlerInterceptor**：离线 Maven 仓无 `aspectjweaver`。
  审计本质需求是「集中记录 + 零业务侵入」，拦截器同样满足。**先问需求本质，再选手段。**
- **日志权限不用 `@PreAuthorize`**：那会让普通检验员查不到自己的记录。
  改为分级数据范围（人人查自己 / `log:view` 跨用户），服务层强制。
  **权限注解解决「能不能调接口」，解决不了「能看哪些行」。**
- **「禁 mock」的边界是「一切用户可见数字」，不只是业务页面**——壳层（顶栏铃铛）最易漏。

## ⚠️ Git 踩坑（高价值）

`.git` 缺 **91 个对象**（6 commit + 多 tree/blob），`git status/branch` 直接报错。

- **`git fetch` 修不好**：本地 `refs/remotes/origin/*` 指向旧 hash，git 据此告诉远端「这些我都有了」
  → 远端不发送 → 拉完仍缺，报 `did not send all necessary objects`。
  **本地对象库损坏时，增量 fetch 会被自己的错误 ref 误导。**
- **解法**：`git clone --mirror <url> lims_recovery.git` → 拷 `objects/pack/pack-<new>.*` 回来
  → **删过期 `multi-pack-index`** → `git fsck --full` = 0 missing / 0 broken。
- **副坑**：给 `git clone` 传绝对 POSIX 路径（`/c/Users/...`）**静默什么都不做**（退出码 0、目录不存在）；
  用相对路径才成功。
- **网络判断**：`curl https://github.com` = `000`，但 `git ls-remote` 完全正常。
  **判断能否推送只看 `git ls-remote`。**

## 验收（实测）

后端 **113/113**；前端 lint 0 / vue-tsc 0 / build ✅；
**Edge + CDP 真实浏览器遍历 20 页 → 0 console error / 0 网络失败**；
**三档分辨率（1440×900 / 1920×1080 / 1366×768）无横向溢出**；
操作日志端到端 6 断言（含 403 失败留痕、伪造 `operator` 无效）。

---

## 修复用户实测反馈的 4 类问题（2026-09-14 第二轮）

用户以 R3 水产检验员等账号实测反馈：工作台报「业务概览加载失败」、点质量分析 404、
录入数据没地方录、下载导入模板无响应、报告生成不了。

**诊断方法**：逐角色接口普查（6 账号 × 各自菜单首屏接口）+ Edge/CDP 复现真实操作路径。
**关键结论：3 个代码缺陷 + 2 个演示数据缺口，处理方式完全不同。**

| 类型 | 问题 | 修复 |
|---|---|---|
| 缺陷 | 工作台无条件调 `/stat/overview`（需 stat:view）→ 403 当错误显示 | 权限感知：只请求有权限的接口；无权限区块用**中性空态**；KPI 按权限过滤；Hero 按钮按权限显隐 |
| 缺陷 | 无权限页面落 404 | 守卫在 catch-all 命中时回查 `ROUTE_BY_PATH`，已登记但无权限 → 改判 **403** |
| 缺陷 | 模板下载空页面 | `target=_blank` + dev server 对 `.xlsx` 返回**空 Content-Type** → 改用 HTML **`download` 属性** |
| 缺陷 | 顶部待办面板刷 15 条 403 | 待办源声明 `permission`，**发请求前**过滤（权限来自 /me，不该用 403 发现） |
| 数据 | R3 无样品可录入 | 新增 `db/seed/03_demo_flow_seed.sql`：S10/S30/S40/S60/S70 各 1 条演示样品 |
| 数据 | 报告只给「重打印」 | 同上（补 S80→S70 链路）；另补「河蟹」项目库 5 项使样品 2 可套库 |

**验证**：113/113 单测；前端 lint/tsc/build 全过；真实浏览器 R3 工作台 0 红错、
点质量分析落 403、**实际录入保存 0/4→75%**、模板**真实落盘 6106 bytes**、
报告审核放行红线生效、**console 0 错误 / 0 失败请求**。

**顺手清理**：`frontend/dist*` 累积 14 个构建产物目录共 36 MB（均已 gitignore）。
