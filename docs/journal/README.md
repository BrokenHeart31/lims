# 📔 工作日记索引（docs/journal/）

> 依据 `AGENTS.md` 第 2.5 节「工作纪律三件套」第 1 件：**每次会话必须写日记**。
>
> 日记不是流水账，只写**有判断价值**的内容：为什么这样做、排除了什么方案、哪些判断被数据推翻、踩了什么坑、有什么可复用结论。
>
> 命名：`YYYY-MM-DD-<agent>-<主题>.md`

## 为什么要有这个目录

本项目除了交付 LIMS 系统本身，还要交付**一套可复现的经验资产**：让后人以此为模板，
「一步到位」复现同类项目。代码只能回答「做了什么」，日记回答「**为什么这么做、当时怎么想的、错在哪**」——
后者才是无法从代码里读出来的部分。

## 日记清单

| 日期 | 作者 | 主题 | 关联任务 | 核心价值 |
|---|---|---|---|---|
| 2026-09-11 | GLM | 角色调整落地 + 判定引擎白名单草案 | T-901/T-902 | 治理矛盾的识别与修法；真实数据形态推翻纸上假设 |
| 2026-09-11 | Copilot | 4070ea6 误删事故修复 + T-902 裁决定稿 | 事故/T-902 | 误删根因与自己记录坑的对应；裁决如何落地 |
| 2026-09-11 | GLM | 工作纪律三件套建立 + D5 前提推翻 + T-903 | T-903/T-904 | D5 数据前提被实测推翻；检索先行的制度化 |
| 2026-09-11 | GLM | **T-401 项目分解全链路** | T-401/T-905 | **「执行前先证伪前提」方法论**；快照下沉 / 预览不落库 / 覆盖式保存三原则；MP ServiceImpl 单测双技巧 |
| 2026-09-11 | GLM | **T-501 检验任务安排域** | T-501 | 三级分配规则（分类/资质/兜底）+ 候选资质过滤 + 沙箱坑深化（commit hash 末位被错写；bash heredoc 中文括号 syntax error） |
| 2026-09-11 | GLM | **UI「Aurora Glass」主题 + V3 fail-loud + 治理二次调整** | T-906/T-907/T-908 | **视觉回归发现 el-tag 过渡卡死（opacity:0）真实缺陷**；`SIGNAL` 三要点；参考第三方 UI 的**许可合规红线**；「华丽 ≠ 堆装饰」的适配判断 |
| 2026-09-12 | GLM | **T-601 判定引擎 + T-701 审核签发 + T-911/T-912 裁决** | T-601/T-701/T-911/T-912 | 闭集白名单矩阵 + 双白名单退回流 + 放行红线；「未录入 ≠ 待判定」的语义切割；MP 实体 update 忽略 null 的坑 |
| 2026-09-12 | GLM | **T-913 前端 UI 结构/空间重整** | T-913 | 保留「Aurora Glass」美感、聚焦结构与一致性；6 公共组件抽取；token 扩展 |
| 2026-09-13 | GLM | **T-105/106/107/603/803 全链路交付** | T-105~107/T-603/T-803 | **ECharts 选型四问与路由级体积验证**；「零依赖」要读上下文；数据权限落点是服务层闭环；**同名字段不同格式（漏 `@JsonFormat`）是最隐蔽的前端坑**；统计指标必须有「无数据」第三态；端口占用导致新接口 404 的误判风险 |
| 2026-09-13 | GLM | **T-702/T-801/T-802 交付 + 沙箱推送打通** | T-702/T-801/T-802/T-915 | **沙箱凭据链为空导致 push 静默挂起**（不该逐层试 TLS 开关）；**`git commit` 后 ref 文件整个未创建**的补写手法；自指陷阱（在交接文档硬编码本轮 hash → 连写 5 轮补正） |
| 2026-09-13 | GLM | **T-916 动态路由（路径注册表 + 中间件转换）** | T-916 | **catch-all 按注册顺序匹配 → 「点击正常、F5 变 404」**；为何 glob 必然失败（两种 path 约定矛盾）；路由与菜单**同源产出**；DB 只存结构不存组件路径的三条理由 |

## 配套技能库

本目录的「可复用结论」段应同步沉淀为 `.agents/skills/` 下的可执行技能：

| 技能 | 用途 |
|---|---|
| `lims-stage-delivery` | **业务阶段全链路交付标准流程**（验证前提 → 契约 → 建表 → 后端 → 单测 → 前端 → 门禁 → 收工三件套 → 推送） |
| `sandbox-git-push` | 沙箱内 git 提交/合并/推送的坑与规避（含**递归删除用 .NET `Directory.Delete`**、端口占用排查、**ref 文件整个未创建**、**凭据链为空的判定与 PAT 用法**） |
| `vue3-dynamic-routing` | **Vue3 动态路由可操作流程**（注册表 vs glob 判断 → 五步落地 → 四个高频坑 → 验证 Checklist） |
| `lims-ui-overhaul` | 前端 UI 结构重整流程（token → 公共组件 → 外壳 → 页面迁移） |
| `judge-engine` | 判定引擎模板（闭集白名单矩阵 + fail-loud 留痕） |
| `rbac-backend` / `mybatisplus-crud` / `vue3-crud-page` / `excel-import` | 分层实现模板（`rbac-backend` 已补「维护界面失效模式防护清单」） |

## 配套知识库（docs/knowledge/）

| 文件 | 主题 |
|---|---|
| `2026-09-11-*`（5 篇） | 判定依据裁决 / Excel 导入选型 / 判定引擎白名单 / 样品状态机 / UI 设计调研 |
| `2026-09-12-*`（3 篇） | 判定引擎研究 / 省平台导出格式 / UI 组件库 |
| `2026-09-13-echarts-integration` | **ECharts 按需引入 + 主题跟随 + 路由级体积验证** |
| `2026-09-13-rbac-maintenance-guardrails` | **RBAC 维护界面 8 类失效模式与防护** |
| `2026-09-13-statistics-api-patterns` | **统计接口模式（聚合 SQL / 第三态语义 / 补零月 / 保留字）** |
| `2026-09-13-dynamic-routing-registry` | **动态路由「路径注册表 + 中间件」三层分离 / 为何不用 glob / catch-all 陷阱 / 落地五步** |

## 阅读建议

- 想了解**为什么某个技术选型**：查 `docs/knowledge/`（结论层），再查本目录同日日记（推理层）
- 想了解**某个坑怎么避**：搜本目录「⚠️ 踩坑记录」
- 想接手某个模块：查该模块相关日记的「可复用结论」段
