# DECISIONS.md（决策记录）

> 格式：日期 | 决策 | 理由 | 决策人

| 日期 | 决策 | 理由 | 决策人 |
|---|---|---|---|
| 2026-09-10 | 新旧库并存原则（AGENTS.md 0.1） | 旧 lims.sql 风格禁止复用，数据走 V1 迁移脚本单向清洗进新表 | 全员 |
| 2026-09-10 | 分支策略 agent/xxx → develop → main | 多 Agent 防冲突，保留每周 main 固化考核 | 全员 |
| 2026-09-10 | 项目根目录定在 `C:\Users\Chen\Desktop\lims`（非手册原写的 `D:\test\lims`） | 用户指定实际路径 | 豆包记录 |
| 2026-09-10 | 实际仓库位置修正为 `D:\lims`（桌面路径被安全软件拦截 git 写入） | 覆盖上一条，与 HANDOFF/STATUS 一致 | 豆包（GLM 顺手修正） |
| 2026-09-10 | T-003 前端骨架由 GLM 完整实现，含 package.json/vite.config/tsconfig/router 等公共文件，Copilot 终审 | TODO T-003 明确指派 GLM，与 AGENTS.md 2.1 所有权表冲突；按任务指派执行并留痕待审 | GLM |
| 2026-09-10 | 认证域前端实现按 AGENTS.md 4.1/8.3 约定先行，JSON 字段命名暂定 camelCase（accessToken/refreshToken），以 Copilot api-spec.md 终审为准 | 契约未落地但骨架需先行；契约变更影响面收敛在 src/api/auth.ts + src/stores/auth.ts | GLM |
| 2026-09-10 | 前端 token 存 localStorage（key=lims_access_token），401 由 axios 拦截器统一清除并跳 /login?redirect= | 禁止 Token 入 URL；刷新 token 机制待 T-002 后端设计 | GLM |
| 2026-09-10 | 认证域契约终审：采纳 camelCase（accessToken/refreshToken、user/permissions/menus），新增 expiresIn/user.deptId/MenuNode.parentId；JSON 字段一律 camelCase | 与 T-003 前端实现一致，前端零改动；camelCase 为 JSON 通例 | Copilot |
| 2026-09-10 | 共享检验员账号定为 njsa000（说明书"njsa00"系笔误；其密码列三行同为 njna000 按"账号名即初始密码"理解） | 与 njna000/njxa000 命名对齐，AGENTS.md 7.4/8.1 为准 | Copilot |
| 2026-09-10 | 后端引入 Lombok（BOM 管理 1.18.34），maven-compiler-plugin 显式声明 annotationProcessorPaths | 减少样板代码；高版本 JDK 下保证注解处理稳定生效 | Copilot |
| 2026-09-10 | 后端统一 server.servlet.context-path=/api | 与前端 vite 代理（/api→8080 无 rewrite）对齐，契约路径直观 | Copilot |
| 2026-09-10 | Spring Boot 定版 3.3.2（不追 4.x） | AGENTS 约束 Spring Boot 3.x；MP 3.5.7 对 Boot 4 兼容性未验证；本地 .m2 已缓存 | Copilot |
| 2026-09-10 | 安全层 401/403 返回 HTTP 状态码 + 统一响应体；业务异常 HTTP 200 + body.code | 前端 request.ts 双通道均已处理；语义清晰 | Copilot |
| 2026-09-10 | 基础表 DDL（basis/customer/dept）由豆包按 6.1 规范先落 db/init/01_basic_tables.sql，product_lib/product_lib_item 作为 T-401 草稿一并落库供 V1 迁移 | T-103/T-104 需可跑；product_lib* 为 S 级领域草稿，Copilot 终审可改 | 豆包 |
| 2026-09-10 | T-201 前端按约定契约 /api/task/*（page/{id}/POST/PUT/DELETE，camelCase，分页 records/total）先行 | Copilot 任务域契约未落地；调整面收敛在 src/api/task.ts | 豆包（代 GLM） |
| 2026-09-10 | 前端引入 ESLint 9 扁平配置（eslint.config.js），新增 devDeps eslint/typescript-eslint/eslint-plugin-vue/globals，npm run lint 门禁 | AGENTS 第 9 章要求 npm run lint；T-004 补齐 | 豆包（代 GLM） |
| 2026-09-10 | RBAC 表采用 sys_ 前缀（sys_user/sys_role/sys_menu/sys_user_role/sys_role_menu），部门复用业务 dept 表并补 parent_id | user 为 MySQL 函数名须转义，sys_ 前缀（RuoYi 惯例）规避；dept 已被 01 建为业务表，加列比重建安全 | Copilot |
| 2026-09-10 | 业务字典字段（任务性质/区域级别/抽样环节/任务状态等）以 VARCHAR 存中文字典值；样品状态机 S10→S90 强制 TINYINT+枚举 | 字典值源自下达文书需原样展示打印，值域稳定；6.1 的 TINYINT 规则针对状态机类枚举（T-301 起严格执行） | Copilot |
| 2026-09-10 | product_lib_item 增加 judge_type（1=限量比较 2=不得检出/不得使用 3=文本/感官人工），V1 按 stdValue 形态推导；T-401 表结构就此定稿 | T-601 自动判定引擎（AGENTS 7.3 规则 1/2/3）直接消费，避免引擎每次重判文本形态 | Copilot |
| 2026-09-10 | JWT 过滤器每请求按 username 从 DB 装配 LoginUser，token 内 perms claim 仅作签发快照不作鉴权依据；R100 在代码层 isAdmin 短路拥有全部权限/菜单 | 权限变更即时生效（踢人/改权无需等 token 过期）；小系统 DB 查询开销可接受 | Copilot |
| 2026-09-10 | 路由守卫对已登录未加载 me 的导航先 await fetchMe | 保证 v-permission 指令在页面渲染前拿到权限数据，避免按钮被误移除 | Copilot |

| 2026-09-10 | V1 迁移源旧表以 customer_legacy/dept_legacy 后缀导入（lims.sql 旧 customer/dept 与 01 新建表同名异构），V1 源表同步改名；lims.sql 本身不入库 | 同库并存前提下旧表与新表同名会覆盖/1050 冲突；_legacy 后缀最小改动，第 5 节校验仍读新表 | 豆包（实测发现，Copilot 终审通过 2026-09-11） |
| 2026-09-11 | application.yml 的 JDBC url 中 characterEncoding 用 utf8（Java 字符集名），禁用 utf8mb4 | Connector/J 8 报 Unsupported character encoding 'utf8mb4'；库表 utf8mb4 由 DDL/连接协商保证 | Copilot |
| 2026-09-11 | 追认 AGENTS.md 角色表修订：GLM=首席架构师/后端核心/终审（S 级），Copilot=前端主力/常规 CRUD（A 级）；Copilot 分支以 `agent/copilot` 为准（首页表格"agent/gpt"为笔误，豆包下轮统一） | 与实际分工一致；分支名以 0.3 节和实际远程分支为准 | Copilot |
| 2026-09-11 | 后端引入 **EasyExcel 3.3.4**（com.alibaba:easyexcel），T-301 采样单导入落地，T-802 省平台导出复用同依赖；禁止 Apache POI 裸 API | 方向性定稿落地（docs/knowledge/2026-09-11-excel-import-research.md）：SAX 流式低内存、注解+监听器极简、RuoYi-Vue-Plus 生态标准；POI 直用易 OOM | GLM（T-301） |
| 2026-09-11 | 样品状态字段 `sample.status` 用 **TINYINT + SampleStatus 枚举**（`@EnumValue` 落库 code、`@JsonValue` 出网数字 code、`getStatusLabel()` 派生中文）；流转唯一入口 `common/enums/SampleStatusTransition`（EnumMap 白名单 + 乐观条件 UPDATE） | AGENTS 0.2/7.2 唯一标准；知识库状态机选型定稿落地（禁 Spring StateMachine、禁私增状态/跳态、禁魔法数字） | GLM（T-301） |
| 2026-09-11 | 样品域权限标识用 **`sample:import` / `sample:confirm` / `sample:query`**；此前交接留言中的 `sample:list` 表述作废（api-spec 已更正） | 以 seed `sys_menu`（menu 31/32/33）与 AGENTS 8.2 实际值为准；`/me` 返回的即 `sample:query`，接口 `@PreAuthorize` 必须同值方可鉴权通过 | GLM（T-301） |
| 2026-09-11 | 采样单导入**部分失败不回滚**（合法行入库 S10，错误行逐条报「行号+原因」）；以 `sample_import_batch` 表登记 Excel A1 文件标记，同标记重复导入整文件拒绝 | 业务允许修正后重导（说明书采样单流程）；A1 标记防重复导入系说明书明确要求（「A1列自定义Excel文件标记，防止重复导入」） | GLM（T-301） |
| 2026-09-11 | 样品表名用 **`sample_info`**（非 `sample`） | 实测发现 `SAMPLE` 是 SQL 关键字（TABLESAMPLE），与 MyBatis-Plus 分页插件所用 JSqlParser 冲突：`SELECT ... FROM sample` 解析失败，导致分页 count SQL 无法优化（WARN + 大表走子查询 count）。与本项目 `user`→`sys_user` 同一处理原则；接口路径 `/api/sample/*` 与权限标识 `sample:*` 不受影响 | GLM（T-301） |

## 2026-09-11 角色调整（用户决策，GLM 执行落地）

| 日期 | 决策 | 理由 | 决策人 |
|---|---|---|---|
| 2026-09-11 | **S 级 + A 级执行权全部归 GLM**（GLM 与 Copilot 同级）；Copilot 只保留三类不可替代工作：① api-spec 契约起草协助/终审；② 规则裁决（判定口径与跨模块语义歧义的最终解释）；③ diff 审查（合并进 develop 前的代码审查） | 用户决策：GLM 能力覆盖 S/A，Copilot 额度应集中在判断类工作；「GLM 实现，Copilot 把关」边界最清晰，避免所有权悬空 | 用户（GLM 落地） |
| 2026-09-11 | 覆盖 2026-09-11「追认 AGENTS.md 角色表修订」（Copilot=前端主力/常规 CRUD）——该条作废 | 上条已被本次角色调整取代；前端主要所有者回归 GLM | 用户（GLM 落地） |
| 2026-09-11 | AGENTS.md 2.1 文件所有权：`backend/` 核心与 `frontend/`、公共文件 → **GLM 独有**；`docs/api/api-spec.md` → GLM 起草 + **Copilot 终审**；冲突基准：公共文件以 GLM 版本为基准，契约以 Copilot 终审版本为基准 | 消除原表「backend 核心 Copilot 独有」与 2.3「S 级仅 glm」的自相矛盾 | GLM |
| 2026-09-11 | AGENTS.md 2.3 重写：删除「GLM/豆包遇到 S 级问题停止」矛盾表述，改为「S/A = GLM，B = 豆包，Copilot = 契约+裁决+审查」；2.4 冲突基准同步修订；7.3 标题改为「GLM 实现于后端，Copilot 裁决口径」；0.2 判定规则实现者由 Copilot 改 GLM | 同上，四份治理文件口径必须一致 | GLM |
| 2026-09-11 | `prompts/glm.md` 与 `prompts/copilot.md` **全文重写**（原两份文件角色名交叉错写：glm.md 自称 copilot、分支写 agent/copilot；copilot.md 分支写 agent/glm） | 提示词是 Agent 的开工依据，错写会导致分支误提交与职责越界 | GLM |
| 2026-09-11 | TODO.md 的 T-401 / T-501 / T-601 / T-701 / T-702 / T-801 Owner 由 Copilot 改为 **GLM**；新增 T-901（治理维护）/ T-902（白名单草案）/ T-903（product_name 补全，豆包） | 与角色调整对齐 | GLM |

## 2026-09-11 Copilot 裁决与终审（T-902 / T-301 契约）

| 日期 | 决策 | 理由 | 决策人 |
|---|---|---|---|
| 2026-09-11 | **修复 4070ea6 误删事故**：该提交误删 backend/db/docs/frontend 共 118 个文件并将 4 个垃圾文件（空 .gitkeep 被改成中文碎片文件名）入库，且已推送远程三分支。以修复提交（agent/copilot `e476cf6`）前滚恢复，不重写历史 | 事故树已推送 main/develop/agent/glm，force-push 改写受保护历史风险大于收益；修复提交完整恢复 fbe8062 内容 | Copilot |
| 2026-09-11 | **T-902 D1 裁决：采纳**——数值结果 < 最低检出限视同「未检出」；jt1 限量比较型直接判合格；lower_limit 为 NULL 时不做视同 | AGENTS 7.3 规则 4 落实；与实测反推口径一致 | Copilot |
| 2026-09-11 | **T-902 D2 裁决：采纳**——「不得检出/不得使用」型按「数值结果 ≥ 检出限才算检出」：未检出或低于检出限 → 合格，≥ 检出限 → 不合格；lower_limit 为 NULL 且数值结果 → 待判定（禁默判合格） | 20 条旧不合格样例反推 + GB 2763 通行解释；白名单最关键口径 | Copilot |
| 2026-09-11 | **T-902 D3 裁决：采纳含补充**——参考项（is_reference=1）单项结论照常计算并标注「参考」展示，但不计入整体结论；全部项目均为参考项时整体 = 待判定 | AGENTS 7.3 规则 5「参与计算但标注参考」与规则 6 的歧义裁决：规则 6「任一单项」仅指非参考项 | Copilot |
| 2026-09-11 | **T-902 D4 裁决：采纳补全矩阵**——stdValue=`--`：未检出（含视同）→ 合格；数值且 ≥ 检出限或检出限 NULL → 待判定 | 与旧 prj_detail 实测口径（`--`+未检出→合格）一致，堵住无依据默认合格的洞 | Copilot |
| 2026-09-11 | **T-902 D5 裁决：采纳**——T-401 由 GLM 做一次性 judge_type 订正脚本（按 stdValue 文本形态重判 jt2/jt3，含 before/after 统计）；引擎运行时只读 product_lib_item.judge_type，禁止依赖旧 prj_detail | lib 表现存 judge_type 全 1 与旧数据存在 jt2 形态不符；运行时依赖旧表违反 0.1 | Copilot |
| 2026-09-11 | 说明书第 2 页两处矛盾样例（铅 0.1 对 ≤0.25 判不合格；氯霉素未检出判不合格）确认为旧系统数据瑕疵，不作为规则依据 | 与白名单口径矛盾；裁决全文见 docs/knowledge/2026-09-11-judge-engine-whitelist.md | Copilot |
| 2026-09-11 | **api-spec 样品域（第 3 章）终审通过**：5 接口路径/方法/权限标识（sample:import/confirm/query）与 Controller、seed、前端 api/sample.ts 逐字段一致；唯一调整为 3.1 审计字段行补 `updatedBy` | T-301 遗留终审关闭；契约以本文档为准 | Copilot |

## 2026-09-11 T-401 项目分解（自动套库）实现决策（GLM）

| 日期 | 决策 | 理由 | 决策人 |
|---|---|---|---|
| 2026-09-11 | **套库预览不落库**：`GET /item/match/{sampleId}` 仅返回初稿，不写 `sample_item` | 业务允许分解员在自动套库初稿上人工增删调整；「套库初稿」与「最终确认的分解结果」是两个概念。若套库即落库，人工调整须先删再写，语义混乱且污染审计字段 | GLM（T-401） |
| 2026-09-11 | **保存为覆盖式**：`PUT /item/save` 先逻辑删除既有明细再全量重建，不提供增量 patch 语义 | 分解页是整体工作台，前端已保证项次连续唯一（1..N）；覆盖式让后端逻辑最简，并消除「前端删了一行但后端未收到」的中间态 | GLM（T-401） |
| 2026-09-11 | **标准库字段快照下沉**：`unit`/`basis_code`/`methods`/`std_value`/`judge_type`/`is_reference`/`lower_limit`/`method_note` 八个字段从 `product_lib_item` **复制**进 `sample_item`，而非只存 `lib_item_id` 外键 | ① 国标会更新，报告必须固化「检验当时的判定依据」，事后查报告不能被新标准追溯篡改；② 分解员人工调整后的值必须独立于标准库，若只存外键则人工改动无处安放 | GLM（T-401） |
| 2026-09-11 | **推论（约束 T-601）**：判定引擎**只读 `sample_item`**，**禁止回溯 `product_lib_item`** | 上条的必然推论。已写入契约 4.1、`SampleItem` 实体注释与 `06_item_tables.sql` 表注释 | GLM（T-401） |
| 2026-09-11 | **项次唯一性用三列唯一键** `uk_sample_item_order (sample_id, item_order, deleted)` | 带 `deleted` 以兼容逻辑删除——否则删掉第 2 项后再新增第 2 项会撞唯一键。前端在删除行后调 `resequence()` 重排为连续 1..N | GLM（T-401） |
| 2026-09-11 | **确认流转用乐观条件 UPDATE**：`WHERE id=? AND status=旧值`，`updated==0` 时抛「样品状态已变更，请刷新后重试」 | 防止并发下重复确认 / 状态被他人推进后仍强行流转；与 `SampleStatusTransition.assertTransition(S20, S30)` 双保险 | GLM（T-401） |
| 2026-09-11 | **`pagePending` 用 `Collectors.groupingBy` 一次性统计 itemCount**，不做逐行 count | 避免 N+1 查询；分页列表最多 100 行，批量聚合成本可忽略 | GLM（T-401） |
| 2026-09-11 | **D5 前提被实测推翻 → 提出裁决请求 #1（T-905）**：`product_lib_item` 3728 行 `std_value` 100% 纯数值；源 `lib` 表同样 100% 纯数值（非迁移漏迁）；`prj_detail` 的 220 条 `不得检出` 仅 5 个兽残项目名且在 `product_lib_item` 中 0 匹配（两表不同源，`lib` 仅覆盖农残 GB 2763-2021）。故 `V3__correct_product_lib_item_judge_type.sql` 实测为**零变更（no-op）**，定位改为「可重跑的口径校验器」。建议 **R1（推荐）** 采纳。全文：`docs/knowledge/2026-09-11-adjudication-request-d5.md` | 执行前先证伪前提：裁决文档给出的是「规则」，但规则作用的「数据」可能根本不存在。若照写脚本，V3 会成为永远输出 0 变更的静默失败，比报错更危险 | GLM 提出，**待 Copilot 裁决**（2 次配额之第 1 次） |
| 2026-09-11 | **T-903 数据补齐方式**：V2 脚本以 `product.id = product_lib.product_code` 一对一匹配，只填 NULL 值（幂等可重跑） | 92 行一对一完美匹配，无歧义；只填空值保证脚本可反复执行而不覆盖人工修正 | GLM（T-903） |
| 2026-09-11 | **工作纪律三件套制度化**（用户强制）：①工作日记 `docs/journal/YYYY-MM-DD-<agent>-<主题>.md`（目标/做法/心得/踩坑/进度/可复用结论）②进度百分比（固定权重：业务主干 55% + 前端 15% + 数据 10% + 质量 10% + 工程化 10%）③动手前先检索（`.agents/skills/` → `docs/knowledge/` → `docs/journal/` → 上网）④经验资产化。写入 AGENTS 2.5 节，2.2 开工五步→六步 | 用户要求「以后的 agent 都要保持这个习惯」，并使项目经验可作为模板复现 | 用户（GLM 落地） |
| 2026-09-11 | **提交纪律（事故教训）**：GLM 提交前必须 `git status --short` 逐项核对暂存区 | 4070ea6 误删事故根因即「未核对暂存区」；已写入 AGENTS 第 12 章红字 | GLM |

## 2026-09-11 Copilot 裁决（T-905，裁决配额第 1 次）

| 日期 | 决策 | 理由 | 决策人 |
|---|---|---|---|
| 2026-09-11 | **T-905 D5 修订：采纳 R1**——D5 前提（lib 存在 jt2/jt3 形态、judge_type 全 1 属迁移缺陷）被实测证伪；judge_type=1 对现存 3728 行是正确值。V3 定位改为「可重跑口径校验器」，零变更即存档证据；**校验器必须 fail-loud**（应然≠实然且 UPDATE 后仍不一致须报错退出，禁止静默通过） | GLM 全库核对证据链完整（docs/knowledge/2026-09-11-adjudication-request-d5.md）：std_value 100% 纯数值、源表 lib 同样、prj_detail 不得检出仅 5 个兽残项目且 0/5 匹配、两表不同源 | Copilot |
| 2026-09-11 | **R3 否决，不开任务**——禁止从旧 prj_detail 反建兽残标准库；5 个兽残「不得检出」项目登记为已知数据覆盖缺口，将来由业务方提供标准文本后走 /api/base 补录 + 重跑 V3 | 标准库权威来源是标准文本而非历史检验记录；反建行缺 basis_code/方法/限量值，不能用于正式判定 | Copilot |
| 2026-09-11 | **追认 T-401「快照下沉」对 D5 的强化**：判定引擎只读 sample_item（快照），禁止回溯 product_lib_item / prj_detail | 国标更新不追溯篡改已出报告；人工调整独立于标准库。取代原 D5「只读 product_lib_item.judge_type」表述 | Copilot |
| 2026-09-11 | **T-601 测试基线裁定**：引擎单测用构造数据（直接造 sample_item 行，不走标准库匹配）覆盖 jt2/jt3 全分支；当前生产数据触发不到 ≠ 可删除白名单分支 | 引擎单测测判定逻辑闭集，与标准库覆盖度无关；20 条旧不合格样例仅作构造蓝本 | Copilot |

## 2026-09-11 用户二次决策 + 本轮工程决策（GLM 自行裁决，第 2 次角色调整）

| 日期 | 决策 | 理由 | 决策人 |
|---|---|---|---|
| 2026-09-11 | **GLM 自行裁决机制（2.6 节）**：判定口径 / 数据形态歧义 / 跨模块语义冲突一律由 GLM 自裁并落档，**不再挂起等待 Copilot**；自裁证据标准不得低于原 Copilot 裁决定稿（可复现证据 + 落档 DECISIONS/knowledge + 反例说明 + fail-loud 优先）；Copilot 降为**可选复核**，裁决配额上限 2 次（已用 1 次，剩 1 次），且**不得作为推进阻塞** | 用户要求「接下来尽量不依赖 copilot 裁决完成项目，方便我启动一次完整项目测试……同时也方便你重整思路」；GLM 已具备完整证据链能力（T-905 实证即为范例） | 用户（GLM 落地） |
| 2026-09-11 | **豆包协作分工清单（2.7 节）**：文档整理、seed/演示数据、省平台模板（T-802）、报告版式样本整理、字段逐项比对校对、迁移脚本执行与条数校验记录 → 可委派豆包；`backend/` 代码、状态机、判定引擎、`db/init`+`db/migrations` 的**内容设计**、api-spec 契约、裁决类工作 → **禁止委派** | 用户要求「期间杂事你可以交给豆包处理」；目的是让 GLM 专注 S/A 主线，同时避免低能力模型触碰核心逻辑 | 用户（GLM 落地） |
| 2026-09-11 | **UI 设计基准「Aurora Glass」**：暗色沉浸 + 极光配色 + 折射玻璃，参考 Mineradio 的视觉语言**并独立实现**；令牌统一 `--lims-*`（`frontend/src/styles/tokens.css`）；三类表面 `.lims-glass` / `.lims-glass-refract` / `.lims-panel` 用途不可混用；**数据密集区禁用折射玻璃** | 用户要求「项目总体 UI 美观参考 mine radio」；暗色玻璃能显著提升观感，但强模糊会损害小字号数字可读性，故必须分区治理 | 用户（GLM 落地） |
| 2026-09-11 | **许可合规红线**：参考第三方 UI **必须先查许可**；Mineradio 为 **GPL-3.0**，本项目**只借鉴设计思路与参数关系，禁止逐字拷贝其代码/CSS/资源**（否则仓库被 GPL 传染，实训项目无法交付） | 工程合规底线；已写入 AGENTS 5.1 与 `docs/knowledge/2026-09-11-ui-design-mineradio-research.md` 第 2 节 | GLM |
| 2026-09-11 | **强调色与按钮底色分离**：`--lims-accent`（青 `#00F5D4`）只做光晕/描边/激活条；按钮/CTA 用 `--lims-brand-gradient`（深端 `#0b7f7a` → 蓝 `#2442ff`，白字对比度 ≥ 4.4:1） | 直接用浅青做按钮底会导致白字看不清（对比度约 2.7:1），是「华丽但不可用」的典型陷阱 | GLM |
| 2026-09-11 | **V3 落地为 fail-loud 口径校验器**（执行 T-905 裁决 R1）：`db/migrations/V3__correct_product_lib_item_judge_type.sql` 归一化改为**派生表统一计算 + NULL 安全比较 `<=>`**（避免 SET/WHERE 双写漂移、避免 `judge_type IS NULL` 行因 `UNKNOWN` 漏更新）；末尾加 `SIGNAL SQLSTATE '45000'` 断言，drift>0 或越界>0 即报错中止（退出码 1） | 裁决硬性要求「禁止静默通过」；实测验证双路径：正常 3728 行零变更 → 通过(exit 0)；人为制造 drift=1/outside=1 → 报错明细 + exit 1；再跑 V3 → 归一化并恢复 exit 0 | GLM（执行 T-905 裁决） |
| 2026-09-11 | **el-tag 全局禁用入场过渡**（实测缺陷修复）：表格内 `el-tag` 会卡在 `el-zoom-in-center-enter-from`（opacity:0），导致「分解进度」「状态」两列**完全空白**；修法为 CSS 统一 `transition: none !important` + `enter-from/active` 强制 opacity:1 | 标签在**异步数据到达后**才挂载，Vue 的双 rAF `nextFrame` 回调被打断时不执行，过渡类永不摘除 → 元素永久 opacity:0。`transition:none` 会让 Vue 判定「无过渡」并立即摘除类名（已用探针页验证类名消失、opacity=1） | GLM |


### T-501 关键设计（2026-09-11 GLM）

| 日期 | 决策 | 理由 | 决策人 |
|---|---|---|---|
| 2026-09-11 | **三级分配规则顺序固定为 分类→资质→兜底**，assignType 数字语义固定为 0=未指派 / 1=分类 / 2=资质 / 3=人工改派；人工改派（assignType=3）永不覆盖 | 规则顺序固定利于后续审计与单测；assignType=3 不覆盖避免「主管手改又被自动分配覆盖」的痛点 | GLM（自裁） |
| 2026-09-11 | **detail.candidates 仅含资质者**，不出现「无资质者作为候选」；reassign 必须从 candidates 中选或传具资质工号，否则 400 | 与 AGENTS 7.4 「仅允许指派有资质者」一致；前端下拉不出错选项 | GLM（自裁） |
| 2026-09-11 | **tester_method 0 行视为数据缺口而非实现缺陷**，auto 自然落到 pending 兜底；不写「自动创建资质」的兜底逻辑 | 反建资质会破坏数据真实性；业务方补录即可恢复，文档化在契约 5.0 | GLM（自裁） |
| 2026-09-11 | **状态机推进必经白名单 + 乐观条件 UPDATE**：S30→S40 走 SampleStatusTransition.assertTransition + baseMapper.update(entity, WHERE id=? AND status=30)，updated==0 抛「样品状态已变更，请刷新」 | 与 T-301 / T-401 一致；防止高并发场景下「以为流转了实际没流转」 | GLM（沿用既有约定） |
| 2026-09-11 | **pagePending 用内联 IN 一次统计 assignDone**，避免 N+1（每样品一次 COUNT sample_item WHERE sample_id=? AND assign_status=1 AND deleted=0） | 列表页性能护栏；样品量大时不能 N+1 | GLM（自裁） |
| 2026-09-11 | **沙箱坑扩展（commit hash 末位错位 + bash heredoc 中文括号 syntax error）**：commit 后必跑 `git rev-parse HEAD` + `git fsck --lost-found` 双验证；ref 错位时用真实 hash 覆盖；commit message 走临时文件 `git commit -F <file>` | 已踩两次；预防第三次 | GLM（沉淀） |

## 2026-09-12 T-601 结果录入 + 自动判定引擎（GLM 自裁，执行期发现并修复 1 个真实缺陷）

| 日期 | 决策 | 理由 | 决策人 |
|---|---|---|---|
| 2026-09-12 | **编号勘误：交接留言（HANDOFF 2026-09-11 21:30）中的「T-602」即 TODO 的阶段六任务，权威编号为 `T-601`**（结果录入 + 自动判定引擎 + S50→S60）。本次所有代码/契约/文档统一按 **T-601** 落档 | TODO.md 为任务清单权威（按业务七阶段对齐编号），且 STATUS 2026-09-11 进度评估亦写 T-601；「T-602」系口误沿用。历史条目（HANDOFF 21:30、journal 09-11、memory 09-11）保持原样不改写 | GLM（自裁） |
| 2026-09-12 | **判定引擎实现形态：不引入任何规则引擎/表达式引擎**，用「闭集白名单矩阵 + `BigDecimal.compareTo` + 闭集外一律待判定 + WARN」实现 | 判定语义固定且规模 < 10 条，Rete/议程调度等机制零回报；**最关键**：`std_value` 来自标准库（数据），若用 Aviator/SpEL/DRL 当表达式求值，等于让数据获得执行语义 → 污染即不可预测结论、无法静态审计。**规则必须是代码，判定依据才是数据**。全文 `docs/knowledge/2026-09-12-judge-engine-research.md` | GLM（T-909 侦察后自裁） |
| 2026-09-12 | **引擎与编排分层**：`service/judge/JudgeEngine` 为**无状态纯函数**（无 IO、无注入依赖），Service 只做「读快照 → 调引擎 → 落库 → 流转 → 聚合」 | 判定矩阵可用构造数据穷举覆盖单测（T-902 裁决要求的测试基线因此廉价）；编排用 Mock 验证。实测：引擎 34 项 + 编排 15 项，全绿 | GLM（自裁） |
| 2026-09-12 | **结果表 `sample_result` 一项一行 + 覆盖式 upsert**（唯一键 `(sample_item_id, deleted)`），存 `test_value`（原始值）/ `conclusion` / `conclusion_source` / `judge_basis`（判定依据说明） | ALCOA+：原始值（人为输入）与派生结论分层保存，报告上每个结论可回放到「哪条规则 + 哪个原始值 + 谁录的」；重复保存走 UPDATE，修订由审计字段留痕，不产生第二行 | GLM（自裁） |
| 2026-09-12 | **判定依据参数不冗余存放**：`std_value`/`judge_type`/`lower_limit`/`is_reference` 一律取自 `sample_item` 快照 | 避免两处真相不一致；延续 T-401 快照下沉 + 白名单定稿 D5「引擎只读 sample_item」 | GLM（自裁） |
| 2026-09-12 | **整体结论聚合只用非参考项**，且「全为参考项 → 待判定」；参考项单项结论照常计算并展示 | 落实 AGENTS 7.3 规则 6 与白名单 D3 裁决；参考项不作放行依据 | GLM（自裁） |
| 2026-09-12 | **整体结论只在保存/提交时重算回写 `sample_info.conclusion`；明细查询实时重算返回** | 列表取持久化值（便宜），明细/提交取实时聚合（权威）；避免 GET 产生写副作用 | GLM（自裁） |
| 2026-09-12 | **存在「待判定」项不阻断提交**（整体结论如实为待判定），放行红线交给 T-701 审核/签发 | 待判定成因可能是数据缺口（缺检出限/缺标准文本），阻断会让样品永久卡在 S50——「用流程阻断掩盖数据问题」；fail-loud 的正确落点是**绝不自动判合格**，而非阻断流程 | GLM（自裁） |
| 2026-09-12 | **形态与判定类型矛盾时判「待判定 + WARN」，不做推测**（如 jt2 配数值型标准值、jt1 配「不得检出」、judgeType 越界） | 矛盾数据意味着上游有误，交人工比猜测安全；同时保证闭集外无静默路径 | GLM（自裁） |
| 2026-09-12 | **缺陷修复（自行发现）：标准值形态关键词与检验值形态关键词必须用各自常量**——标准值是「**不得**检出」，检验值是「**未**检出」 | 初版复用了同一常量，导致 jt2 全部分支退化为「待判定」（6 个单测同时报红）。已各自定义常量并补文档与技能防复发 | GLM（实测发现） |
| 2026-09-12 | **工具纪律补充：同一文件的多次编辑必须串行** | 一条消息内对同一文件并行发多条 Edit 会「基于旧内容写回」互相覆盖（报成功但改动消失），表现为编译报「找不到符号」。已写入 `sandbox-git-push` 技能规则 7 | GLM（实测发现） |

## 2026-09-12 Copilot 复核终审（T-601；复核轮零实现代码）

| 日期 | 决策 | 理由 | 决策人 |
|---|---|---|---|
| 2026-09-12 | **T-601 复核终审：通过**。追认：判定矩阵与 T-902 D1/D2/D4/D5 逐格一致；D3 聚合（参考项不入整体、全参考项→待判定）正确；「不得检出/未检出」各自常量（GLM 自查缺陷修复固化）；BigDecimal.compareTo 全覆盖；原始值/派生值分层落库；快照只读（grep 对 product_lib_item/prj_detail 零回溯命中）；双保险流转；「待判定不阻断提交」有决策留档；契约第 6 章与 Controller/DTO/VO/前端逐字段一致 | 证据全部独立实测：mvn test 85/85 复跑 BUILD SUCCESS；`git show --stat 887067e` 37 文件与申报一致无删除项；判定域回溯禁令 grep 零命中。终审全文见 `docs/journal/2026-09-12-copilot-t601-review.md` | Copilot |
| 2026-09-12 | **保留意见不开新裁决、转 TODO 交 GLM 自裁**：T-911（分页参数双轨 vs 0.3 统一约定，倾向修订 0.3 承认现状）与 T-912（空值结果行可提交至 S60，「已录入」口径待裁，T-701 开工前必须先裁） | 二者均不构成安全洞或口径错误：T-911 章节内自洽运行无碍；T-912 与「待判定不阻断、红线在审核」的既定自裁逻辑自洽。用户已明确降级期间 Copilot 不做裁决阻塞主线（AGENTS 2.6），且 T-912 需对照业务说明书原文裁——属 S 级语义裁决，留强模型轮次 | Copilot |
| 2026-09-12 | **T-501 历史申报勘误**：AssignServiceImplTest 实为 **13 项**（当时申报 14）；T-601 提交对该文件 diff=0，未触碰 | 本轮 mvn test 实测 13；定向 `git diff fd6897b..887067e` 0 行排除本轮改动 | Copilot |

## 2026-09-13 剩余范围（T-702 / T-801 / T-802 / T-603 / T-803）设计与裁决（GLM 自裁，交付总监拍板）

> 背景：经豆包核对，说明书尚有四块未实现：报告生成打印（第九节）、查询（第十节）、省平台上报（第十一节）、
> 基础数据与系统管理维护界面、检验员任务导出、可视化图表。本轮先攻业务主线缺口（T-702 + T-801 + T-802 + T-603）。
> 依据：业务说明书 docx 逐节提取（报告版式已逐格还原）+ `docs/knowledge/2026-09-12-province-export-format.md`。

| 日期 | 决策 | 理由 | 决策人 |
|---|---|---|---|
| 2026-09-13 | **报告不落快照，实时聚合**：`ReportVO` 由 `sample_info + sample_item + sample_result + sample_audit_log + sys_user` 实时组装，不新建报告数据表 | S80 已签发后样品**再无写路径**（S90 为终态、无回退边），数据天然冻结，实时聚合不会漂移；落快照反而引入「快照 vs 事实」两处真相的一致性风险与维护成本。**反例排除**：若将来允许 S90 回退改数据，则必须改为落快照并加版本号——该前提变更需先改 AGENTS 7.2 | GLM（自裁） |
| 2026-09-13 | **电子签名「占位 + 可配置」**：签名取 `sys_user.signature_url`；该列为空时报告渲染**虚线占位框（框内印姓名）**，**绝不生成/伪造签名图片** | 说明书原文要求「报告无制表、审核、批准人签字无效」——伪造签名是资质红线。当前库中无签名图片资源，占位方案既满足版式完整（签字位可见）又不越线。新增列而非硬编码路径，便于用户后续上传 | GLM（自裁） |
| 2026-09-13 | **报告机构/资质信息配置化**：`lims.report.*` 承载机构名/CMA 号/CATL 号/地址/电话/邮编/传真/7 条注意事项/实验环境条件；默认值取自说明书内嵌报告样例（CMA=`181004090030`、CATL=`[2020]农质检核（苏）字第0021号`） | 资质证书号与主体信息随复评换证变化，写死要改代码发版。**默认值是「说明书样例的两行资质」推断**：`181004090030` 为 CMA 资质认定证书编号，`[2020]农质检核（苏）字第0021号` 为 CATL 考核合格证书号——与报告样例顶部两行标识一一对应。⚠️ 生产必须由使用单位核对覆盖；已在 `application.yml` 注释中明示 | GLM（自裁，证据=docx 报告样例） |
| 2026-09-13 | **报告类型差异仅在资质行**：`CMA` 显示 cma-no 一行；`CMA_CATL` 显示 cma-no + catl-no 两行 | 说明书只说「根据资质选择生成相应类型」，未给出两种报告的版式差异；从样例顶部两行标识反推，差异最小化实现，避免臆造版式 | GLM（自裁） |
| 2026-09-13 | **省平台导出范围阈值 = `status >= 80`**（含 S80 已签发） | 报告已签发即构成可上报的**最终结论**；S90 只是报告落盘动作，不构成上报门槛。**反例排除**：仅 `=90` 会漏掉「已签发但尚未打印」的样品，而上报数据本身只依赖结论不依赖报告文件 | GLM（自裁，采纳豆包倾向） |
| 2026-09-13 | **省平台导出严格 10 列、不插空隔列**；**参考项（`is_reference=1`）不加 `*` 前缀** | 旧系统把 10 个逻辑列放在 A/B/C/D/F/H/J/L/N/P（E/G/I/K/M/O 空列）是**表格合并的视觉残留**，省平台按列语义读取；插空列会让下游解析依赖列位置而非表头，反而更脆。`*` 是纸质报告的读者提示，结构化上报数据不应混入标点 | GLM（自裁） |
| 2026-09-13 | **省平台导出支持 `?taskNo=` 筛选参数** | 说明书原文「样品信息可以按任务编号进行筛选」；无参数时全量导出已完成样品 | GLM（自裁） |
| 2026-09-13 | **在检查询的「停留时长」不新建状态流水表**，改用既有字段按状态近似推导（`createdAt/confirmedAt/updatedAt/MAX(assigned_at)/MAX(sample_result.updated_at)/auditAt`） | 增量边界：新建 `sample_status_log` 需要回填全部历史状态迁移（无历史数据可回填），且要在 5 个既有 Service 的每次流转处埋点——改动面远超「查一下停留多久」的收益。近似推导对「找出卡住的样品」这一真实用途足够，差异点已写入契约 9.1 供后人判断。**反例排除**：若将来要做流程时效 SLA 统计，则必须补流水表 | GLM（自裁） |
| 2026-09-13 | **查询域的 `itemTotal/enteredCount/pendingCount/abnormalCount` 强制复用 `ResultEntryPolicy`**，禁止新域另写判定 | T-912 已把「有效录入」定为唯一口径；若查询域另写一套，「已录入数」在两个页面会给出不同答案——这类「同名词不同义」是数据类系统最隐蔽的 bug 源 | GLM（自裁） |
| 2026-09-13 | **新增权限标识 `stat:view`**（质量分析/统计报表），同步 `AGENTS.md` 8.2 与 seed `sys_menu` id=841 | 统计看板是独立信息域（看趋势）而非查询（找样本），复用 `query:*` 会造成权限语义混淆；新增一个标识成本极低，且 AGENTS 8.2 本就是权限唯一权威（同轮同步更新，不留双轨） | GLM（自裁） |
| 2026-09-13 | **统计接口不在后端加缓存**，前端 60s 缓存 + 刷新按钮；零新增依赖（图表库选型另在 T-803 批次裁决） | 演示/中小规模数据下 SQL 聚合足够快；后端加缓存会引入失效策略与「看到旧数」的困惑。**反例排除**：数据量上万后再引入 Caffeine/Redis，届时按需加 | GLM（自裁） |
| 2026-09-13 | **不做行级数据权限**（部门仅作组织树展示） | 说明书只要求「用户/角色/权限分类」，未提数据范围权限（如「只能看本部门样品」）；凭空实现会与既有 49 个权限标识体系冲突且无验收依据 | GLM（自裁） |
| 2026-09-13 | **⚠️ MySQL 保留字坑**：`generated` 是 MySQL 8 保留字，SQL 别名/列名不可直接用（`SUM(...) AS generated` 报 1064） | 本轮写 `V6` 校验 SELECT 时踩到，改用 `cnt_generated`。已写入技能备忘，防止后续统计 SQL 复发 | GLM（实测发现） |

## 2026-09-13 T-803 图表库选型 + T-105/106/107/603 落地决策（GLM 自裁）

> 背景：T-105（方法-检验员资质）/ T-106（项目标准库）/ T-107（系统管理 4 页）/ T-603（检验员任务查询）/
> T-803（可视化看板）为说明书要求但非七阶段主线的剩余范围。前四者属既有域的维护界面，
> T-803 需新选图表库（前一轮遗留「图表库选型待新裁决」）。

### T-803 图表库选型（本轮核心裁决）

| 日期 | 决策 | 理由 | 决策人 |
|---|---|---|---|
| 2026-09-13 | **选定 ECharts 5.5.1**（`echarts@^5.5.1`），**按需引入**（`echarts/core` + 显式 `echarts.use([...])`），**封装为 `components/common/LimsChart.vue`** | 见下方「选型四问」 | GLM（自裁） |
| 2026-09-13 | **只用 Canvas 渲染器，不引 SVG 渲染器** | 本系统图表为常规统计图（无海量 DOM 交互需求），Canvas 性能更优且体积更小；SVG 渲染器会额外增加约 30KB 且无收益 | GLM（自裁） |
| 2026-09-13 | **不引 `echarts/features`（LabelLayout / UniversalTransition 等）** | 环形图 emphasis 缩放 + 折线动画已由 `charts` 模块自带；额外 feature 会拉大体积，实测当前图表零使用 | GLM（自裁） |
| 2026-09-13 | **图表颜色一律从 CSS 变量读取（`getComputedStyle`），禁止硬编码色值** | 项目有「禁止硬编码色值以保证换肤能力」的既定令牌纪律（tokens.css 头部注释）；图表作为页面的一部分若写死颜色，切到 `lims-theme-light` 时会出现「暗色图表配浅色页面」的割裂。取色在**函数调用时**而非模块加载时，保证主题切换后重新取色 | GLM（自裁） |
| 2026-09-13 | **ECharts 实例按路由懒加载分包**（`analysis.vue` 动态 import，实测 chunk 542.83 kB / gzip 183.12 kB，**不进入主包**） | 主包已 1.27 MB（gzip 411 kB），图表库必须隔离。实测确认 `analysis-*.js` 为独立 chunk，未进入 `index-*.js`，首屏零成本 | GLM（实测验证） |
| 2026-09-13 | **`LimsChart` 用 `ResizeObserver` 而非 `window.onresize`** | 侧栏折叠（224px ↔ 68px）不触发 window resize，但图表容器宽度会变；用 `window.onresize` 会导致折叠侧栏后图表错位不重绘 | GLM（自裁） |
| 2026-09-13 | **`ResizeObserver` 回调内先判 `clientWidth/clientHeight > 0` 再 `resize()`** | 容器被隐藏（tab 切换、抽屉收起）时宽高为 0，此时调用 `resize()` 会触发 ECharts 控制台警告且可能算出 `NaN` 尺寸 | GLM（自裁） |
| 2026-09-13 | **`setOption(option, true)` 强制 notMerge** | 数据集整体替换（如 Top N 从 5 项变 3 项）时，`merge` 模式会残留上一批的维度，图表出现幽灵数据。notMerge 语义与「每次都是全量数据」的接口设计一致 | GLM（自裁） |
| 2026-09-13 | **空数据不画坐标系，直接渲染 `AppEmpty`**；`empty=true` 时销毁 ECharts 实例 | 「禁 mock 假数据」的视觉延伸——没数据就说没数据，而不是画一个空的坐标轴让人以为「加载失败」。同时避免空图表占用 canvas 内存 | GLM（自裁） |
| 2026-09-13 | **页面用 `Promise.allSettled` 而非 `Promise.all` 并行取 8 个统计** | 单个接口异常不应导致整页空白：失败的图表自然落到空态，其余照常展示。`all` 会在首个 reject 时中断，让「一个接口挂了」升级为「整页不可用」 | GLM（自裁） |

**选型四问（为什么是 ECharts，而不是 Chart.js / 手写 SVG / AntV G2）**

1. **是否需要零新增依赖？** —— 前一轮 DECISIONS 写的「零新增依赖」是**当时**（省平台导出/查询域）的约束，
   同时明确留了「图表库选型另在 T-803 批次裁决」的口子。手写 SVG 可以零依赖，但本页需要
   环形图 + 横向条形图 + 纵向柱图 + 双线趋势图共 7 个图表，手写 SVG 的坐标计算、tooltip、
   hover 高亮、图例交互全部要自研——**代码量与维护成本远超引入一个成熟库**，且自研版本必然缺
   「数值标签避让」「长标签截断」等已解决问题的细节。
2. **本地是否可离线安装？** —— 已实测 `npm install echarts@5.5.1` 成功（3 个包：echarts + zrender + tslib，
   约 1 分钟）。仓库 `node_modules` 已就位，离线构建无阻。
3. **生态与文档是否够后人接棒？** —— ECharts 是中文文档最完整的图表库，且是 RuoYi-Vue-Plus 等
   国内 Vue 后台框架的默认选择（本项目多处选型与该生态对齐：EasyExcel、MyBatis-Plus）。
   后人在 `docs/knowledge/` 阅读本决策后，可直接复用同类项目的图表配置经验。
4. **体积是否可控？** —— 按需引入实测：整个 `analysis` 路由 chunk 542.83 kB（gzip 183.12 kB），
   且**不进入主包**（主包 `index-*.js` 保持 1.27 MB）。主包体积零增长是本决策可接受的关键前提。
   > 对比：若用 `import * as echarts from 'echarts'`（全量引入），chunk 会膨胀到约 1 MB 以上。

**反例排除**：
- **Chart.js**：体积更小（约 200KB），但中文文档较少，且环形图 + 图例滚动的默认表现不如 ECharts；
  本项目是教学/交付双用途，ECharts 的中文生态对后人更友好。
- **AntV G2**：语法更声明式（图形语法），但学习曲线陡于 ECharts 的 option 配置，
  且与项目现有「配置驱动」的代码风格（`chartOptions.ts` 返回纯 option 对象）不同路。
- **手写 SVG**：见第 1 问，7 个图表自研成本过高，且无 tooltip/图例交互。
- **完全不做图表，只做数字表格**：说明书明确要求「可视化看板」，表格满足不了「看趋势」的需求。

### T-105 / T-106 / T-107 / T-603 落地决策

| 日期 | 决策 | 理由 | 决策人 |
|---|---|---|---|
| 2026-09-13 | **T-603「已录入」判定强制复用 `ResultEntryPolicy`**，`MyTaskVO` 未录入时 `conclusion/conclusionSource/conclusionLabel` **一律置 null**（而非返回 DB 中的 `PENDING`） | 与 `ResultDetailVO.Item` 同一处理原则（见 2026-09-12 T-912 决策）。若直接返回 DB 值，前端会把「检验员还没录」误显示成「待判定」，两种状态在页面上无法区分 | GLM（自裁） |
| 2026-09-13 | **T-603 数据范围由服务层 `resolveTesterScope()` 强制收敛**：`MyTaskQueryDTO.testerScope` 是服务层注入字段，**无请求绑定** | 说明书要求「检验员查询到安排给自己的任务」；若把 `testerNo` 做成前端可传参数，客户端改一个参数就能看到别人的任务——数据权限必须在服务端闭环。DTO 上有字段但无 `@RequestBody`/`@RequestParam` 绑定，伪造无效 | GLM（自裁） |
| 2026-09-13 | **T-603 查询下界 `status >= 40`** | 只有进入「任务安排完成」之后的样品才构成检验员待办；S10~S30 阶段的样品尚未分配，出现在「我的任务」里会让人困惑 | GLM（自裁） |
| 2026-09-13 | **T-105 导入幂等键 = `(methodName, testerNo)`，命中则更新** | 说明书未定义重复导入行为。选「更新」而非「拒绝」的理由：资质状态会变化（有效↔失效），用户重新导入一份修正过的 Excel 是自然操作；拒绝会迫使用户逐条手改。`errors` 数组返回逐行原因，部分失败不回滚（与采样单导入 T-301 既有口径一致） | GLM（自裁） |
| 2026-09-13 | **T-106 明细采用「覆盖式替换」（C10），不做差异比对** | 明细无业务唯一键，单产品通常 < 50 项。覆盖式实现更简单且天然幂等（同样输入重复提交结果一致）；差异比对需要额外的键设计与冲突处理，收益为负 | GLM（自裁） |
| 2026-09-13 | **T-106 导入为「一对多覆盖式」**：同一产品编号的多行 → 旧明细全删 → 重建 | 与 C10 一致。EXCEL 是产品的完整快照而非增量补丁，覆盖式与用户心智模型吻合 | GLM（自裁） |
| 2026-09-13 | **T-106 删除产品时若含明细则拒绝** | 与「删除部门时若有子部门/用户则拒绝」同一原则：静默级联删除明细会让用户丢失已整理好的项目库数据，且无回收路径（逻辑删除只删产品头会让明细成为孤儿） | GLM（自裁） |
| 2026-09-13 | **T-106 判定字段一致性校验（`validateJudgeConsistency`）为硬校验**：jt1 必填 `stdValue`；judgeType 越界直接 400 | 这些字段是 `JudgeEngine` 的直接输入（快照下沉后判定只读 `sample_item`）；落库前不拦住矛盾数据，异常会在 T-401 项目分解 + T-601 判定时才暴露，排查链路极长。fail-loud 前置到录入点 | GLM（自裁） |
| 2026-09-13 | **T-107 重置密码独立接口 `PUT /sys/user/{id}/password` + 独立 DTO** | 密码是单向写入（BCrypt），不能与普通字段混在同一 update：若与 `SysUserSaveDTO` 合并，「编辑资料」请求体一旦缺 `password` 就会误改/清空密码。独立接口 + 独立 DTO 在类型层面杜绝误用 | GLM（自裁） |
| 2026-09-13 | **T-107 `SysUserVO` 不含 `password`/`salt` 字段**（类型层面，非 `@JsonIgnore` 过滤） | AGENTS §1「VO 禁止出现 password/salt」。用注解过滤存在「有人误删注解就泄露」的风险；类型里根本没有该字段，泄露在编译期即不可能 | GLM（自裁） |
| 2026-09-13 | **T-107 `username` 创建后不可修改**；更新请求中的 `username`/`password` 被忽略 | 登录名是审计线索的锚点（`created_by`/`operator` 等都引用它），可变会让历史记录指向错误的操作者 | GLM（自裁） |
| 2026-09-13 | **T-107 用户自锁保护（fail-loud）**：不能停用/删除自己；不能停用/删除**最后一个启用中的 R100 用户**（`countActiveAdmins`） | 这是 RBAC 维护界面最危险的失效模式——管理员误操作把自己锁在系统外，且没有其他 R100 账号可以救回来，只能改数据库。返回具体原因而非静默放行 | GLM（自裁） |
| 2026-09-13 | **T-107 R100 角色三重保护**：`roleCode` 不可改、权限绑定请求直接跳过、不可删除 | R100 是「无需 `sys_role_menu` 即拥有全部权限」的特权角色（`SysRole.ADMIN_ROLE_CODE`）。改码会留下已授权数据指向错误角色；写权限绑定是无意义的（不生效），**显式忽略比静默写入更诚实**；删除会导致全系统失管 | GLM（自裁） |
| 2026-09-13 | **T-107 角色有用户绑定时拒绝删除**，并返回剩余绑定人数 | 静默删除会产生「无角色用户」——能登录但没有任何权限，且用户自己无法理解原因。返回人数引导先解绑 | GLM（自裁） |
| 2026-09-13 | **T-107 菜单形态校验三件套为硬门禁**：`validateParent`（成环检测，`guard < 64`）/ `validatePermission`（唯一性前置拦截）/ `validateTypeShape`（按钮必有 permission、目录菜单不应有、菜单必有 path） | 成环会让树构建进入死循环（栈溢出）或产生孤儿节点；`permission` 有 DB UNIQUE 索引，前置拦截是为了给出**可读错误**而非抛 SQL 异常；形态错误（如按钮无权限标识）会导致权限体系静默失效——界面上有按钮但永远勾不出权限 | GLM（自裁） |
| 2026-09-13 | **T-107 菜单删除时级联清理 `sys_role_menu`** | 关联表无逻辑删除；不清理会产生孤儿行，让 R100 之外的角色权限树出现「指向不存在菜单的勾选态」——用户勾了但它无效，且无法解释 | GLM（自裁） |
| 2026-09-13 | **T-107 `SysMenuVO`（含按钮行）与登录导航 `MenuVO`（仅目录/菜单）分离** | 用途不同：权限分配界面需要看到按钮权限行才能勾选细粒度权限；导航树显示按钮行是无意义的噪音。两个 VO 各自 `from()` 工厂，避免一个 VO 带两套语义 | GLM（自裁） |
| 2026-09-13 | **补齐 `base:lib:add`/`base:lib:edit`/`base:lib:remove` 三个权限种子**（`sys_menu` id 832/833/834），并授权 R2 | 实测发现 T-106 的 Controller 已用这三个标识做 `@PreAuthorize`，但 seed 中只有 `base:lib:list`——不补则**非 R100 用户永远无法新增/修改/删除项目库**（403）。这是「权限标识在代码中使用但未在种子中定义」的典型缺口，靠 R100 硬编码权限掩盖了，普通角色一测就现形 | GLM（实测发现） |

## 2026-09-12 T-911 / T-912 裁决 + T-701 实现决策（GLM 自裁）

| 日期 | 决策 | 理由 | 决策人 |
|---|---|---|---|
| 2026-09-12 | **T-911 分页参数双轨：保留双轨、不追溯改，在 api-spec 0.3 明确「新域一律用 `current`/`size`」**，`pageNum`/`pageSize` 仅标注为 task(2.2)/sample(3.3) 的历史兼容写法 | ①响应体两域一致（`records/total/current/size`），前端解包逻辑已统一；②item/assign/result 三域与前端已端到端验证通过，改名为「无功能收益的破坏性变更」，还会让 T-601 刚通过的终审失效；③真正风险是「下一棒不知道该用哪套」——0.3 现已给出唯一答案 | GLM（自裁，方向由 Copilot 提出） |
| 2026-09-12 | **T-912「已录入」口径定稿**：一个检测单项算已录入 ⇔ `testValue` 非空白 **或** 文本/感官项（jt3）已人工选定合格/不合格（实现于 `service/result/ResultEntryPolicy`，唯一权威）；`submit` 的录齐校验按此口径 | 业务说明书第八条要求「检测数据**全部录入**系统后，样品即转入签发流程」——**空值行不是检测数据**。旧口径「存在一行结果就算已录入」允许空值行一路提交到 S60，属流程卫生缺口 | GLM（自裁，对照说明书原文） |
| 2026-09-12 | **「未录入」与「待判定」必须严格区分**：未录入（空值/无结果行）= 检验员**操作缺漏** → `submit` **阻断**；待判定（`testValue` 有值但引擎判不出，如缺检出限/标准文本）= **数据缺口** → **不阻断**（沿用 2026-09-12 既有自裁），改由审核页显式确认后放行 | 两者混同会导致两种恶果：把「未录入」当「待判定」会让操作缺漏被静默放过；把「待判定」当「未录入」会把样品永久卡死。fail-loud 的正确落点是「绝不静默判合格」，不是「用流程阻断掩盖数据缺口」 | GLM（自裁） |
| 2026-09-12 | **`ResultDetailVO.Item.entered` 由派生 getter（`conclusion != null`）改为真实字段**；未有效录入时 **conclusion 相关字段一律不出网** | 旧实现无法表达「空值行」这一状态——空值行的 `conclusion=3` 会被前端显示成「待判定」，与真正的待判定混淆。不出网 + 前端统一显示「未录入」消除歧义 | GLM（T-912 落地） |
| 2026-09-12 | **状态机退回走独立白名单**：`SampleStatusTransition` 新增独立 `RETURN` EnumMap（仅 `S60 → S50`）+ `assertReturn/canReturn/returnAllowed`；**不并入正向 `VALID`** | 若并入，`assertTransition(S60,S50)` 将全局合法，任何调用方都可能误当普通推进使用；且有审批语义的域里「退回」是更强的约束（必须带原因 + 留痕 + 通知检验员）。单测固化 `assertTransition(S60,S50)` 必须拒绝 | GLM（自裁，承接 Copilot 只读铺垫的建议） |
| 2026-09-12 | **审核流水用独立事件表 `sample_audit_log`（只追加、永不改写）**；`sample_info` 另存**当前有效**的 `audit_by/audit_at/audit_opinion/sign_by/sign_at` 供报告打印 | ALCOA+ Audit Trail：事件表是「谁在何时因何把样品推到什么状态」的唯一证据；主表存当前值避免 T-702 报告合成反查流水（与 `confirmed_by/confirmed_at` 同一先例）。业务依据：报告页脚「报告无制表、审核、批准人签字无效」 | GLM（自裁） |
| 2026-09-12 | **审核退回清空 `sample_info.audit_*` 但保留流水** | 报告上不得出现「未通过的审核人」；而「谁因何退回」是历史事实，必须留在流水里供追溯与责任界定 | GLM（自裁） |
| 2026-09-12 | **放行红线：存在异常项（未录入/待判定）时，审核通过必须带 `abnormalConfirmed=true`**，否则 `code=400`；前端未勾选时「审核通过」按钮禁用；流水记 `abnormal_confirmed=1` | 对接 T-912：审核环节是放行红线的落点。「有异常仍放行」必须是一个**有意识、有留痕**的决定，而不是被忽略的默认值 | GLM（自裁） |
| 2026-09-12 | **异常项清单分 `BLANK`（未录入）/`PENDING`（待判定）两类，排序固定「未录入在前、待判定在后」** | 审核人应先看「根本没数据」的项（必须打回补录），再看「有数据但判不出」的项（可人工裁决）；顺序确定便于人工核对与测试断言 | GLM（自裁） |
| 2026-09-12 | **⚠️ MyBatis-Plus 实体式 `update` 忽略 null 字段**：「清空某列」必须 `LambdaUpdateWrapper.set(col, null)`，`entity.setXxx(null)` 静默无效 | 本轮实现「退回时清空审核信息」时发现；不显式 set 会导致审核人残留、报告署名错误。已写入 `lims-stage-delivery` 原则 4 与状态机知识库 | GLM（实测发现） |
## 2026-09-13 动态路由选型决策（用户决策，GLM 落地）

| 日期 | 决策 | 理由 | 决策人 |

|---|---|---|---|

| 2026-09-13 | **前端动态路由采用「路径注册表 + 中间件转换」**（方案 1），否决「按 DB 菜单直接映射组件路径（glob）」 | 用户决策。契约约束：**不为动态路由大规模改 DB**（不可破坏项）。glob 方案的致命前提是「菜单 path 与 views 目录结构一致」，而实际数据里 `/sample`（目录=文件）与 `/assign/index`（目录=index）两种约定并存且互相矛盾——glob 无法同时兼容，会静默产生 404。注册表把「path→组件」收敛为一个显式常量表，DB 只提供结构，两者解耦 | 用户（GLM 落地） |

| 2026-09-13 | **DB 菜单表只提供结构信息**（title/path/icon/层级/排序），**组件路径不落库** | 三条独立理由：①「组件路径」是构建期概念（Vite 静态分析决定 chunk 划分），落库后运行时无法动态 import；②落库等于允许运营侧任意指定文件路径，是安全边界外扩；③构建产物 hash 化后文件路径与源码路径不一致（本项目实测 `analysis-*.js`），落库路径在生产环境无法解析 | GLM（自裁） |

| 2026-09-13 | **路由与侧栏菜单由 `buildNavigation()` 同源产出**，禁止分别维护 | 分别维护必然漂移：「菜单里有、路由里没有」= 点击 404；「路由里有、菜单里没有」= 页面孤儿。同源产出后两者在构造上不可能不一致（菜单树的每个节点都来自一条成功落地的路由） | GLM（自裁） |

| 2026-09-13 | **catch-all 路由（`/:pathMatch(.*)*`）必须「移除后重加」，不允许在 `staticRoutes` 里一次性注册** | vue-router 4 的 `addRoute` 恒为**追加**，匹配按注册顺序。若 catch-all 先于动态路由注册，则任何动态路径都会先命中 404 —— 表现为**「菜单点击正常（首次 addRoute 后跳转）、F5 刷新变 404（重新走一遍匹配）」** 这种极难定位的现象。封装 `registerNotFound()` 在每次注册完动态路由后调用，保证 catch-all 永远在最后 | GLM（实测踩坑） |

| 2026-09-13 | **无法识别的菜单 path 必须 fail-loud `console.warn`**（收集进 `unresolved`），禁止静默跳过 | 静默跳过会让「菜单配错了」和「功能没做」在界面上完全无法区分，排查成本极高。与项目既有 fail-loud 原则（判定引擎「无法判定」落待判定 + WARN）一致 | GLM（自裁） |

| 2026-09-13 | **历史路径兼容采用 `PATH_ALIAS` 单向映射**（`/sample/register → /sample`、`/assign → /assign/index`），而**不是**改 DB 里的 path 字段 | 用户不可破坏项约束。别名层让「旧链接/旧书签/旧文档里的路径」继续可用，且 DB 与代码解耦：将来 DB 修正后别名可安全删除（映射到同一个 canonical path，重复无害）。本轮虽同时修正了 DB 的 2 条错路径（使之与注册表对齐），但别名层保留为兜底 | GLM（自裁，用户约束） |

| 2026-09-13 | **无页面的 3 条菜单（`/base/basis`、`/base/customer`、`/sys/log`）设 `visible=0` 隐藏**，不删除数据 | 用户决策。这 3 条在 DB 里存在但项目无对应页面（判定依据/客户管理已并入项目标准库维护，日志查看未列入交付范围）。删除会丢失菜单 id 的授权关系（`sys_role_menu` 孤儿行）且不可逆；`visible=0` 是最小改动且可随时恢复。后端 `SysMenuMapper` 已按 `visible=1` 过滤，零代码改动即可生效 | 用户（GLM 落地） |

| 2026-09-13 | **动态路由的路径清洗规则**：剥离 query（`?` 之后）、剥离尾部斜杠、空串归一为 `/`，再查别名表与注册表 | DB 中的 path 是人工维护数据，实测存在尾斜杠与带 query 的写法。清洗放在 `normalizeMenuPath()` 单一入口，保证「注册表命中判断」只面对规范形态 | GLM（实测发现） |

| 2026-09-13 | **图标采用白名单 `ICON_MAP`**，禁止 `(ElIcons as any)[name]` 动态取值 | 动态取值会让 Vite 无法 tree-shake，把 Element Plus 全部 400+ 图标打进主包（实测体积代价巨大）；且拼错的图标名会静默渲染空白。白名单 + `FALLBACK_ICON` 兜底：可 tree-shake，且未知图标有明确视觉降级 | GLM（自裁） |


## 2026-09-14 收尾决策（T-918 操作日志 + 通知去假数据 + 死代码处置）

> 背景：项目进入收尾。用户要求「检查项目还需要做什么、什么是没用的了、
> 已完成的部分有没有漏掉/空壳/未实现完整，所有功能交互是否彻底闭环」。
> 全量盘点后确认：**说明书 13 项业务功能零缺口**，17 个前端页面亦已全部完成；
> 残余问题集中在两处「空壳/假数据」与一处工程债。

| 日期 | 决策 | 理由 | 决策人 |
|---|---|---|---|
| 2026-09-14 | **操作日志用 `HandlerInterceptor` 实现，不引入 Spring AOP** | 本机离线 Maven 仓库无 `spring-boot-starter-aop` / `aspectjweaver`，无法引入依赖；审计的本质需求是「集中记录 + 零业务侵入」，`HandlerInterceptor`（spring-webmvc 已在依赖内）同样满足。**反例排除**：若坚持「标准做法是 AOP」，该需求会因无法下载依赖而整块搁置；且本系统「接口 ↔ 业务动作」近乎一一对应，粒度差异不构成信息损失 | GLM（自裁） |
| 2026-09-14 | **审计绝不记录请求体**（只记 方法 / 路径 / 结果码 / 耗时 / 操作人 / IP） | 请求体可能含密码（登录、改密、重置密码）。记录 body 会让审计表变成凭据泄露渠道。这是硬约束，已写入契约 15.0 与代码注释 | GLM（自裁，安全红线） |
| 2026-09-14 | **审计写入失败不得影响业务响应**：落库整体 try/catch，仅打 WARN | 审计是旁路。让日志表一次写入失败把成功的业务操作变成用户眼里的失败，是把基础设施可靠性问题转嫁给业务 | GLM（自裁） |
| 2026-09-14 | **失败请求同样留痕**（`result=0` + HTTP 状态码） | 审计的价值恰恰在于记录「尝试」；只记录成功等于放弃对异常行为的追溯。已实测：nj003 无权限调 `POST /sys/user` → 403 且入库为「失败」 | GLM（自裁） |
| 2026-09-14 | **操作日志采用分级数据范围**：接口**只要求登录**，不使用 `@PreAuthorize('log:view')`；无 `log:view` 者由服务层强制 `operator = 本人工号` | 日志入口位于用户菜单的「我的操作日志」，每个用户都应能查自己做过什么（个人可追溯性，ALCOA+ 要求）；「跨用户查看」才是特权。**权限注解解决「能不能调接口」，解决不了「能看哪些行」**。实测伪造 `?operator=<他人>` 无效 | GLM（自裁，与 T-603 `testerScope` 同一原则，第二次复用） |
| 2026-09-14 | **只记录写请求**（POST/PUT/DELETE），GET 不入表 | 若把 GET 也写入，日志表会被翻页查询瞬间淹没，真正有价值的写操作反而被埋掉。查询行为由容器访问日志承担 | GLM（自裁） |
| 2026-09-14 | **模块识别用有序前缀表**（`LinkedHashMap`），匹配不到归「其他」；**不硬造模块名** | 顺序敏感：`/report/audit` 必须排在 `/report` 之前，否则日志会把「报告审核」错记成「报告生成」——这类错误显示的是「看起来完全合理的模块名」，界面上极难发现，故用 6 项单测固化顺序语义。硬造模块名会把「路径未登记」这个缺陷伪装成正常数据 | GLM（自裁） |
| 2026-09-14 | **顶部铃铛通知改为「真实待办汇总」**，删除 4 条写死的假通知 | 原实现是假数据（「3 份报告待审核」「样品 JK-2026-001 检铅超标」…），**违反 DECISIONS 2026-09-13「禁 mock 假数据」原则**。假数据比没有数据更危险：用户会照着它去点、去查，然后发现系统里根本不存在这件事。**关键认识：「禁 mock」的适用边界是「一切用户可见的数字」，而不仅是「业务页面」**——壳层（顶栏）最容易漏 | GLM（自裁） |
| 2026-09-14 | **待办数据来源复用既有分页接口的 `total`**（`/report/audit/pending` 等 6 个），**不新增后端接口、不新增字段** | 这些接口已是各业务页的数据源，复用它们保证「待办数字」与「点进去看到的列表条数」永远一致；若另写统计接口，两处口径将来必然漂移 | GLM（自裁） |
| 2026-09-14 | **待办不做「已读」状态**；每个来源用 `Promise.allSettled` 独立容错；**count=0 的条目直接不展示** | 待办是**派生数据**（样品状态变了它就该变），存「已读」只会让角标与事实不符；`allSettled` 保证「某个模块没权限」不会把整个面板打空；显示「0 份待审核」是噪音 | GLM（自裁） |
| 2026-09-14 | **`AppSkeleton.vue` 接入工作台 KPI 加载态**（而非删除） | 它符合 UI 提示词 §二十七「Loading 优先使用 Skeleton」且项目此前 KPI 区无加载态（会「先闪空卡片再填数字」）。用起来比删掉更有价值 | GLM（自裁） |
| 2026-09-14 | **`ProgressBar.vue` 删除**（零引用） | 全站 4 处进度表达已在用 `el-progress`（assign ×2 / result ×2），且用了 `text-inside` 这种 ProgressBar 表达不了的形态。两套进度组件并存 = 未来样式改动要改两处，且新组件零消费方——保留它只是负债。**反例排除**：把它塞进不使用 `text-inside` 的位置属于「为用而用」，会引入无收益的视觉变更 | GLM（自裁） |
| 2026-09-14 | **「上一轮交接说还剩什么」必须以代码复核为准** | 交接单写「T-917-5 还剩 6 页待迁」「P3 列宽待修」，实测均已在本轮之前完成。文档描述的是「当时」，代码描述的是「现在」；照交接单继续「迁移」就是纯返工。**交接文档用于定位，不用于决策** | GLM（实测发现） |

## 2026-09-14 用户实测反馈修复（GLM 自裁）

> 背景：用户以 R3「水产检验员」等账号实测后反馈 4 类问题。
> 诊断的关键动作是**逐角色接口普查**（6 个账号 × 各自菜单对应的首屏接口）+
> Edge/CDP 复现真实操作路径，从而把「真缺陷」与「数据缺口」分开。

| 日期 | 决策 | 理由 | 决策人 |
|---|---|---|---|
| 2026-09-14 | **「没有权限」不等于「加载失败」**：无权限的数据区块一律渲染**中性空态**（说明缺哪个权限、去哪儿办），红色错误告警只保留给「有权限但真的失败了」 | 用错误告警表达权限不足，用户会判定「系统坏了」——本次用户原话「没有权限也不需要这样报错显示啊」即为实证。同时收窄 `overviewError` 语义后，真故障不再被权限噪音淹没 | GLM（自裁） |
| 2026-09-14 | **前端已知的权限，必须在发请求前过滤，不得依赖 403 兜底** | 顶部「待办提醒」挂载即拉 6 个业务接口，无权限账号因此刷出 15 条控制台 `403 Forbidden`。`/me` 已返回权限集合，**请求前过滤是零成本**；靠 403 兜底既吵又掩盖真错误 | GLM（自裁） |
| 2026-09-14 | **首页类跳转按钮一律按权限显隐**（不渲染 = 不误点） | Hero 区「查看质量分析」此前硬编码，检验员点击后命中 catch-all 变 404。**能点的按钮必须真的可达**；不可达的入口不如不给 | GLM（自裁） |
| 2026-09-14 | **404 与 403 必须区分**：路由守卫在 catch-all 命中时回查 `ROUTE_BY_PATH`，若路径是系统内**已登记**的页面则改判 `403`（无权限） | 未授权页面不会被注册进路由，因此天然落到 404；用户看到「功能不存在」会误判为功能缺失。语义准确的错误页能省掉一轮沟通 | GLM（自裁） |
| 2026-09-14 | **同源静态资源下载一律使用 HTML `download` 属性**，禁用 `target="_blank"` | 实测 dev server 对 `.xlsx` 返回**空 Content-Type**，配 `target="_blank"` 时浏览器开个空白标签页后什么都不做。`download` 属性同源时**忽略 Content-Type** 直接保存。**反例排除**：fetch+Blob 方案对无需鉴权的静态资源是过度设计 | GLM（实测发现） |
| 2026-09-14 | **「用户说 XX 用不了」必须先区分「代码缺陷」与「数据缺口」** | 本次 4 个抱怨中 2 个是数据缺口（库里无 S40/S50 待录入样品、无 S80 待生成报告样品）。若当代码 bug 改状态机，会把正确约束改坏；若只回「这是数据问题」，用户依然测不了——**两者都要处理，但方式完全不同** | GLM（自裁） |
| 2026-09-14 | **新增 `db/seed/03_demo_flow_seed.sql`：按角色铺演示待办**（S10/S30/S40/S60/S70 各 1 条），并补齐「河蟹」项目标准库 | 一个只跑通「管理员全流程」的演示库，对「检验员登录后干什么」零信息。**「每个角色至少 1 条属于自己的待办」应作为交付标准**。另：`product_lib` 此前**一条水产条目都没有**，而演示样品多为水产品，套库必失败——属数据覆盖缺口，非代码问题 | GLM（实测发现） |
| 2026-09-14 | **演示种子脚本必须幂等且可反复执行**（先按 `DEMO-` 前缀物理清理，再重建；项目库按 `product_code` 判重） | 用户会反复自测、反复重置；不幂等的种子第二次执行就报错或产生重复数据 | GLM（自裁） |
